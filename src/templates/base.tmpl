{{ define "base" }}
{{ $basePackage := .Package }}
package {{ $basePackage }}

import (
	"net/http"
	"strconv"
	"fmt"
	"slices"
	"net/mail"
)
{{ template "typedef" . }}
{{ template "constructor" . }}

{{ end }}

